server:
  port: 4002
  
spring:
  application:
    name: USER-SERVICE
  datasource:
    username: sa
    password:
    url: jdbc:h2:tcp://localhost:3000/./databases/vechile-users
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      dialect : org.hibernate.dialect.H2Dialect

